# Environment Configuration Example
# Copy this file to .env and fill in your actual values

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/hard-backend

# JWT Configuration (REQUIRED - Generate a secure random secret)
# Use a cryptographically secure random string (at least 32 characters)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-at-least-32-characters-long-here
JWT_EXPIRE=7d

# Email Configuration (for password reset and verification)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3000

# Server Configuration
PORT=5000
NODE_ENV=development

# Optional: Seed Data Passwords (for development only)
# If not provided, secure random passwords will be generated
SEED_ADMIN_PASSWORD=secure-admin-password-for-development
SEED_USER_PASSWORD=secure-user-password-for-development

# Security Notes:
# 1. NEVER commit your actual .env file to version control
# 2. Use strong, unique passwords for production
# 3. Generate JWT_SECRET using a cryptographically secure method
# 4. Use environment-specific values for each deployment
# 5. Regularly rotate secrets and passwords